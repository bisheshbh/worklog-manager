Registration 
INSERT INTO USER VALUES (? , ? , ? , ? , ? , ?)
INSERT INTO TASK VALUES (task_description , created_date , is_edited, used_id)
INSERT INTO FEEDBACK VALUES (comment, created_date , updated_date , task_id)
INSERT INTO DEPARTMENT VALUES (deparment_name)

#Pull out task of current logged in user. 
SELECT department_name , task_description , created_date , updated_date , is_edited FROM USER INNER JOIN TASK ON USER.id=TASK.user_id 
INNER JOIN DEPARTMENT ON deparment.id = user.department_id

#Pull out feedback of the current view task/updates 
SELECT comment , created_date, updated_date from TASK INNER JOIN FEEDBACK ON TASK.id=FEEDBACK.task_id

#Choice to select a department for user 
SELECT department_name FROM DEPARTMENT 

